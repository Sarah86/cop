import styled from 'styled-components'

const Shadow = styled.div`
  box-shadow: 0px 50px 57px -41px rgba(0, 0, 0, 1);
  //box-shadow: 1px 60px 50px -47px rgba(0,0,0,0.65);
  width: 100%;
  position: absolute;
  height: 150px;
  bottom: 0%;
  pointer-events: none;
  z-index: 1;
`

export default Shadow
